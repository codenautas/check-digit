import { checkdigitCompute, CheckDigitParameters, checkQualityOfCodeList } from "../lib/check-digit-compute";

const CONF: CheckDigitParameters = {
    multipliers: [2,3,4,7],
    divider: 11
}

function getList(prefix:string, max:number){

}
